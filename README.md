# Libvirt-friendly definitions for common operating systems

Libvirt, the most common virtualization library on Linux, uses XML to store definitions of virtual machines. 

This repository's goal is to maintain definitions that are optimized for running common operating systems.